/**
 *  save only favourites vehicles to local storage
 *  other way then using ID becuase they don't have unique IDs ? 
 * 
 */

// export default function saveFavouritesToStorage(favourites: any[]) {
//     try {
//         const favsString = JSON.stringify(favourites);
//         localStorage.setItem('favouriteVehicles', favsString);
//     } catch (error) {
//         console.error("Error saving favourites to storage:", error);
//     }
// }